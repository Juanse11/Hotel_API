#!/usr/bin/env node
let { apikey } = require( '../dist/index' );

let size = 40;
if ( process.argv.length > 2 ) {
  size = Number( process.argv[2] )
  if ( !Number.isInteger( size ) ) {
    console.log( `bad size: ${process.argv[2]}` )
    process.exit( 1 );
  }
}

console.log( apikey( size ) );
